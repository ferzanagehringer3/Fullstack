spring.application.name=dalzana

# MySQL DB Configuration (Production/Local)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/db?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=root
spring.profiles.active=local
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

server.port=${SERVER_PORT:8080}

app.upload-dir=uploads
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Email Configuration (Gmail example - replace with your credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:dalzana.connect@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:dydyylnvbabtmjku}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true


